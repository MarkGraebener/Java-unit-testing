import org.junit.Before;
import org.junit.Test;

import java.util.ArrayList;
import java.util.List;

import static org.junit.Assert.*;

public class StudentTest
{
//    private List<String> names;
//
////    @Before causes that method to be run before the @Test method. The @Before methods of superclasses will be run before those of the current class. It's like a default constructor for a Plain Java Object Class, and the name of the method does not really matter, but we suggest to use setUp().
//    @Before
//    public void setUp(){
//        this.names = new ArrayList<>();
//        this.names.add("Fer");
//    }
//
//    // We can verify that the names list is not longer null because it was initialized in the setUp() method with the following test:
//    @Test
//    public void testIfNamesIsInitialized(){
//        assertNotNull(names);
//    }
//
//    @Test
//    public void testIfANameCanBeAdded(){
//        assertEquals(1, this.names.size());
//        this.names.add("Zach");
//        assertEquals(2, this.names.size());
//        assertSame("fer", this.names.get(0));
//        assertSame("Zach", this.names.get(1));
//    }


}
